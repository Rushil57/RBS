using iTextSharp.text;
using iTextSharp.text.pdf;
using Microsoft.AspNetCore.Http;
using Microsoft.Extensions.Options;
using System;
using System.Collections.Generic;
using Xunit;
using plweb.Internals;

namespace plweb.xunit
{
    public class PayStubb_Test
    {
        //[Theory]
        //[InlineData("2019-01-01", "2019-01-31", 1)]
        //public void GeneratePaystub_Test(string _fromDate, string _toDate, int _agentID)
        //{
        //    var FromDate = Convert.ToDateTime(_fromDate);
        //    var ToDate = Convert.ToDateTime(_toDate);
        //    var agent = new AgentAgent();
        //    var payreport = agent.GetPayReport(FromDate, ToDate, _agentID);
        //    //List<TimeclockAction> lsttimeclockActions = new List<TimeclockAction>();
        //    //agent.GeneratePDF(payreport, FromDate, ToDate, lsttimeclockActions, _agentID);
        //    Assert.True(payreport != null);
        //}

    }
}

